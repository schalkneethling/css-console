/**
 * Public entry point for CSS Console.
 *
 * The runtime is not implemented yet. The scaffold exposes one named
 * constant so that the import smoke test can prove that the entry point
 * resolves from the package root. The public API described in
 * plans/implementation.md lands in later issues.
 */

export const PACKAGE_NAME = "@schalkneethling/css-console";
