/**
 * Diagnostic registry seam.
 *
 * The diagnostic contract lives with the record contracts in
 * src/core/records/index.ts. This module houses the diagnostic code
 * registry, which lands with CSSC-004.
 */

export type { Diagnostic } from "../records/index.ts";
