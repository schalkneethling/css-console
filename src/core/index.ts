/**
 * Placeholder for the core module graph. CSSC-003 introduces the public
 * record contracts, and Phase 1 fills the compiler directories.
 */
export type CorePlaceholder = {
  readonly phase: "phase-0";
};
